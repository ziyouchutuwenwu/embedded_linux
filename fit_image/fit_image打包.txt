参考地址
http://www.bankaiyuan.com/t/246

cd $UBOOT
make ARCH=arm CROSS_COMPILE=arm-linux-gnueabi- vexpress_ca9x4_defconfig

code .config，添加fit模式的支持
注意，修改下面为
CONFIG_FIT=y

make ARCH=arm CROSS_COMPILE=arm-linux-gnueabi-

pack_all.sh是打包的例子

现在内存地址仅作参考
	内核在0x80008000
	ramdisk在0x53000000

暂时没有设备测试，等以后拿到设备以后，逐步调试